# Git rules

- Inspect existing repository commit conventions before committing.
- Prefer BEM-style messages: `block__element: description`.
- One atomic commit per meaningful task.
- Never force-push shared branches.
- Do not auto-commit unless `git.auto_commit = true` and policy allows.
